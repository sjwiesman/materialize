---
title: "Live dashboard"
description: "A browser application whose numbers move as the underlying data changes, with no polling."
menu:
  main:
    parent: "examples"
    weight: 40
---

{{% include-headless "/headless/restructure-stub" %}}

A browser application whose numbers move as the underlying data changes, with no polling.

## What this example will hold

Aggregate views over an operational source; a server that relays changes to the browser; a client that applies diffs rather than refetching; the isolation level chosen so that two figures on screen always agree.

## Related

- [Examples](/examples/)
