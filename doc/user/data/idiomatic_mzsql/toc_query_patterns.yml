columns:
  - column: "Query Pattern"
  - column: "Idiomatic Materialize"

rows:
  - Query Pattern: |
      [ANY() Equi-join condition](/model-data/idiomatic-materialize-sql/any/)
    Idiomatic Materialize: |
      [Use `UNNEST()` or `DISTINCT UNNEST()` to expand the values and join](/model-data/idiomatic-materialize-sql/any/).

  - Query Pattern: |
      [`NOT IN (<subquery>)` predicate](/model-data/idiomatic-materialize-sql/not-in/)
    Idiomatic Materialize: |
      [Rewrite to `NOT EXISTS`, or filter out `NULL`s on both sides of the `NOT IN`](/model-data/idiomatic-materialize-sql/not-in/).

  - Query Pattern: |
      [`mz_now()` with date/time operators](/model-data/idiomatic-materialize-sql/mz_now/#mz_now-expressions-to-calculate-past-or-future-timestamp)
    Idiomatic Materialize: |
      [Move the operation to the other side of the comparison](/model-data/idiomatic-materialize-sql/mz_now/#mz_now-expressions-to-calculate-past-or-future-timestamp):

  - Query Pattern: |
      [`mz_now()` with disjunctions (`OR`) in materialized/indexed view
      definitions and `SUBSCRIBE` statements](/model-data/idiomatic-materialize-sql/mz_now/#disjunctions-or):

    Idiomatic Materialize: |
      [Rewrite using `UNION ALL` or `UNION` (deduplicating as necessary)
      expression](/model-data/idiomatic-materialize-sql/mz_now/#disjunctions-or)
